#include<bits/stdc++.h>
using namespace std;
int main()
{
    cout<<"Enter the number of string :";
    int n;
    cin>>n;
    string  str[n];
 
    pair<string, int> p;
    for(auto x : )
    

    for(int i=0;i<n;i++)
    {
        cin>>str[i];
        // pair<string,int>p;
        // p.first=str[i];
        // p.second=i;
        // v.push_back(p);
        v.push_back(make_pair(str[i],i));
    }




}