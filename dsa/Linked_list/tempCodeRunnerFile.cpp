  // insertFirst(head, 12);

    // printlist(head);

    // insertLast(tail, 200);

    // printlist(head);
    
    // int p;
    // cout<<"Enter the position :";
    // cin>>p;

    // insertPos(tail, head, p, 22);
    // printlist(head);